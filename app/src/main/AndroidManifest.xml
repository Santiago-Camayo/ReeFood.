<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.reefood">

    <!-- Permisos deben ir aquí, antes de <application> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReFood"
        tools:targetApi="31">
        <activity
            android:name=".views.MisDonaciones"
            android:exported="false" />
        <activity
            android:name=".views.EditaPerfil"
            android:exported="false" />
        <activity
            android:name=".views.Configuraciones"
            android:exported="false" />
        <activity
            android:name=".views.Donador"
            android:exported="false" />
        <activity
            android:name=".views.VerDonacion"
            android:exported="false" />
        <activity
            android:name=".views.Publicaciones"
            android:exported="false" />
        <activity
            android:name=".views.Menu"
            android:exported="false" />
        <activity
            android:name=".views.Registro"
            android:exported="false" />

        <activity
            android:name=".views.IniciarSesion"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>